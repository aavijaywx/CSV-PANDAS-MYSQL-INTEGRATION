# CSV-PANDAS-MYSQL-INTEGRATION

## Description
Automatically reads a CSV file using Pandas and inserts all data into a MySQL database. Table is created dynamically based on CSV columns.

## Features
- Read CSV files with Pandas
- Automatically create MySQL table based on CSV columns
- Bulk insert all rows into MySQL.
- Works for any CSV file without manually writing insert queries

